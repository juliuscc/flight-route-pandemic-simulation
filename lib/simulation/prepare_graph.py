def prepare_graph(graph):

    print(graph.nodes.data())

    print(list(graph.nodes))

    for node in list(graph.nodes):
        # graph[node]['test'] = 'Monday'
        print(node)
    #     nodes['test'] = 'Monday'

    return graph
